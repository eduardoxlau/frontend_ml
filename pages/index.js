import Item from "components/item";
import { Container } from "ui";

const Home = () => {
  return (
    <Container
      display="flex"
      justifyContent="center"
      pt={90}
      fontSize={20}
      fontFamily="bold"
    >
      Realiza una busquedad
    </Container>
  );
};

export default Home;
